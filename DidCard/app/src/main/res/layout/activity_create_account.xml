<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.didchain.didcard.ui.create.CreateCardViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        tools:ignore="Autofill,MissingConstraints,contentDescription">

        <ImageView
            android:id="@+id/icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/margin_start"
            android:layout_marginTop="63dp"
            android:src="@drawable/logo_img"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/margin_start"
            android:layout_marginTop="16dp"
            android:text="@string/create_account_title"
            android:textColor="@color/color_0c123d"
            android:textSize="24sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/icon" />

        <EditText
            android:id="@+id/password"
            style="@style/PasswordEditTextStyle"
            android:layout_marginTop="16dp"
            android:text="@={viewModel.password}"
            android:hint="@string/create_account_input_password"
            app:layout_constraintTop_toBottomOf="@+id/title" />

        <EditText
            android:id="@+id/confirmPassword"
            style="@style/PasswordEditTextStyle"
            android:layout_marginTop="16dp"
            android:text="@={viewModel.confirmPassword}"
            android:hint="@string/create_account_input_password_again"
            app:layout_constraintTop_toBottomOf="@+id/password" />

        <CheckBox
            android:id="@+id/privacyAuthorityCB"
            android:layout_width="wrap_content"
            android:visibility="gone"
            app:layout_constraintTop_toBottomOf="@+id/confirmPassword"
            app:layout_constraintStart_toStartOf="@+id/confirmPassword"
            android:layout_marginTop="8dp"
            onCheckedChangedCommand="@{viewModel.checkPrivacyAuthrity}"
            android:button="@drawable/privacy_authority_selector"
            android:textSize="12sp"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/privacyAuthorityTv"
            android:layout_width="0dp"
            app:layout_constraintEnd_toEndOf="@+id/confirmPassword"
            app:layout_constraintTop_toTopOf="@+id/privacyAuthorityCB"
            app:layout_constraintBottom_toBottomOf="@+id/privacyAuthorityCB"
            app:layout_constraintTop_toBottomOf="@+id/confirmPassword"
            app:layout_constraintStart_toEndOf="@+id/privacyAuthorityCB"
            android:textSize="12sp"
            android:layout_height="wrap_content" />

        <TextView
            style="@style/YellowButtonStyle"
            android:layout_marginTop="16dp"
            onClickCommand="@{viewModel.clickCreate}"
            android:text="@string/create_account"
            app:layout_constraintTop_toBottomOf="@+id/privacyAuthorityTv" />

        <TextView
            android:layout_width="0dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:text="@string/create_account_tip"
            android:layout_marginStart="@dimen/margin_start"
            android:layout_marginEnd="@dimen/margin_end"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginBottom="70dp"
            android:layout_height="wrap_content" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>